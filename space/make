CC = gcc
CFLAGS = -Wall -Wextra -std=c99
LIBS = -lraylib -lm

SRC = main.c player.c enemy.c bullet.c
OBJ = $(SRC:.c=.o)

game: $(OBJ)
	$(CC) $(OBJ) -o game $(LIBS)

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -f *.o game
